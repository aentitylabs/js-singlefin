import { State } from "../influencer/state";
import { Follower } from "../main";

export class ClientLoginFormState extends State {
    public handle(follower: Follower, trend: string, model: any): void {
        
    }
}